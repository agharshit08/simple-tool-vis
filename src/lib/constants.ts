export const AI_CONFIG = {
  // Maximum number of rows to send to the AI model for context
  // If the dataset has more rows, we take an evenly distributed sample of this size
  MAX_CONTEXT_ROWS: 1000,
};
